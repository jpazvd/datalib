*! XAA_2021_XSA_v01_M_school.do -- harmonize the school module
*
* Reads   Data/Original/raw_school.csv   (as delivered)
* Writes  Data/Stata/XAA_2021_XSA_v01_M_school.dta
*
* Generated by datalib_makelib. The labels below were written FROM the
* generated dataset, so they cannot drift from what it contains.
* The data is synthetic: no value describes a real person or household.

version 15

args here
if (`"`here'"'=="") local here "."

import delimited using "`here'/Data/Original/raw_school.csv", ///
    clear varnames(1) case(preserve)

label data "XAA 2021 XSA v01 master - SYNTHETIC (datalib_makelib)"

label variable schid "School identifier"
label variable region "Region"
label variable urban "Urban school"

* declared structure -- mirrors module_schema: in datalib.yaml
char _dta[datalib_level]      "school"
char _dta[datalib_keys]       "schid"
char _dta[datalib_parent]     ""
char _dta[datalib_parentkeys] ""

save "`here'/Data/Stata/XAA_2021_XSA_v01_M_school.dta", replace
