*! XAA_2021_XSA_v01_M_teacher.do -- harmonize the teacher module
*
* Reads   Data/Original/raw_teacher.csv   (as delivered)
* Writes  Data/Stata/XAA_2021_XSA_v01_M_teacher.dta
*
* Generated by datalib_makelib. The labels below were written FROM the
* generated dataset, so they cannot drift from what it contains.
* The data is synthetic: no value describes a real person or household.

version 15

args here
if (`"`here'"'=="") local here "."

import delimited using "`here'/Data/Original/raw_teacher.csv", ///
    clear varnames(1) case(preserve)

label data "XAA 2021 XSA v01 master - SYNTHETIC (datalib_makelib)"

label variable schid "School identifier"
label variable clsid "Classroom number within school"
label variable tchid "Teacher number within classroom"
label variable subject "Subject taught"
label variable experience "Years of teaching experience"

* declared structure -- mirrors module_schema: in datalib.yaml
char _dta[datalib_level]      "teacher"
char _dta[datalib_keys]       "schid clsid tchid"
char _dta[datalib_parent]     "classroom"
char _dta[datalib_parentkeys] "schid clsid"

save "`here'/Data/Stata/XAA_2021_XSA_v01_M_teacher.dta", replace
