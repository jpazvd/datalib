*! XAA_2021_XSA_v01_M_classroom.do -- harmonize the classroom module
*
* Reads   Data/Original/raw_classroom.csv   (as delivered)
* Writes  Data/Stata/XAA_2021_XSA_v01_M_classroom.dta
*
* Generated by datalib_makelib. The labels below were written FROM the
* generated dataset, so they cannot drift from what it contains.
* The data is synthetic: no value describes a real person or household.

version 15

args here
if (`"`here'"'=="") local here "."

import delimited using "`here'/Data/Original/raw_classroom.csv", ///
    clear varnames(1) case(preserve)

label data "XAA 2021 XSA v01 master - SYNTHETIC (datalib_makelib)"

label variable schid "School identifier"
label variable clsid "Classroom number within school"
label variable grade "Grade taught"

* declared structure -- mirrors module_schema: in datalib.yaml
char _dta[datalib_level]      "classroom"
char _dta[datalib_keys]       "schid clsid"
char _dta[datalib_parent]     "school"
char _dta[datalib_parentkeys] "schid"

save "`here'/Data/Stata/XAA_2021_XSA_v01_M_classroom.dta", replace
