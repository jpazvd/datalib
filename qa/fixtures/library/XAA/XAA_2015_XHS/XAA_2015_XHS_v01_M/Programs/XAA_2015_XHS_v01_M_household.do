*! XAA_2015_XHS_v01_M_household.do -- harmonize the household module
*
* Reads   Data/Original/raw_household.csv   (as delivered)
* Writes  Data/Stata/XAA_2015_XHS_v01_M_household.dta
*
* Generated by datalib_makelib. The labels below were written FROM the
* generated dataset, so they cannot drift from what it contains.
* The data is synthetic: no value describes a real person or household.

version 15

args here
if (`"`here'"'=="") local here "."

import delimited using "`here'/Data/Original/raw_household.csv", ///
    clear varnames(1) case(preserve)

label data "XAA 2015 XHS v01 master - SYNTHETIC (datalib_makelib)"

label variable hhid "Household identifier"
label variable region "Region"
label variable urban "Urban residence"
label variable hh_size "Household size"
label variable hh_weight "Household sampling weight"

* declared structure -- mirrors module_schema: in datalib.yaml
char _dta[datalib_level]      "household"
char _dta[datalib_keys]       "hhid"
char _dta[datalib_parent]     ""
char _dta[datalib_parentkeys] ""

save "`here'/Data/Stata/XAA_2015_XHS_v01_M_household.dta", replace
